class AppSizes {
  static double? width;
  static double? height;
  static double space2pxH = (height ?? 852) * 0.0024;
  static double space2pxW = (width ?? 400) * 0.0051;
  static double pandingForContent = (width ?? 400) * 0.06; //24
}
